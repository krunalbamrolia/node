<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
</head>
<body>
    <h1>Edit <%= book.title %> Book</h1>
    <form method="POST" action="/edit/<%= book._id %>">
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" value="<%= book.title %>" required>
        <label for="author">Author:</label>
        <input type="text" id="author" name="author" value="<%= book.author %>" required>
        <label for="price">Price:</label>
        <input type="number" id="price" name="price" value="<%= book.price %>" required>
        <label for="description">Description:</label>
        <input type="text" id="description" name="description" value="<%= book.description %>" required>
        <label for="edition">Edition:</label>
        <input type="text" id="edition" name="edition" value="<%= book.edition %>" required>
        <label for="aboutAuthor">About Author:</label>
        <input type="text" id="aboutAuthor" name="aboutAuthor" value="<%= book.aboutAuthor %>" required>
        <label for="publish">Publish:</label>
        <input type="text" id="publish" name="publish" value="<%= book.publish %>" required>
        <label for="status">Status:</label>
        <input type="text" id="status" name="status" value="<%= book.status %>" required>
        <button class="button" type="submit">Save Changes</button>
    </form>
    <a class="button" href="/">Cancel</a>
</body>
</html>
 -->

<!-- 
 <!DOCTYPE html>
 <html lang="en">
 <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Edit Book</title>
     <link rel="stylesheet" href="styles.css">
 
     <style>
         body {
             font-family: Arial, sans-serif;
             background-color: #f0f0f0;
             margin: 0;
             display: flex;
             justify-content: center;
             align-items: center;
             height: 100vh;
         }
 
         .container {
             max-width: 600px;
             background-color: #fff;
             padding: 30px;
             border-radius: 10px;
             box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
         }
 
         h1 {
             text-align: center;
             color: #007bff;
             margin-bottom: 20px;
         }
 
         form label, form input {
             display: block;
             width: 100%;
             margin-bottom: 10px;
         }
 
         form input {
             padding: 8px;
             border: 1px solid #ccc;
             border-radius: 5px;
             box-sizing: border-box;
         }
 
         .button-container {
             text-align: center;
             margin-top: 20px;
         }
 
         .button {
             display: inline-block;
             padding: 10px 20px;
             font-size: 16px;
             cursor: pointer;
             text-align: center;
             text-decoration: none;
             outline: none;
             color: #fff;
             background-color: #007bff;
             border: none;
             border-radius: 5px;
             transition: background-color 0.3s;
             margin-right: 10px;
         }
 
         .button:hover {
             background-color: #0056b3;
         }
 
         a.button {
             text-decoration: none;
             display: inline-block;
             padding: 10px 20px;
             font-size: 16px;
             cursor: pointer;
             text-align: center;
             text-decoration: none;
             outline: none;
             color: #fff;
             background-color: #007bff;
             border: none;
             border-radius: 5px;
             transition: background-color 0.3s;
         }
 
         a.button:hover {
             background-color: #0056b3;
         }
     </style>
 </head>
 <body>
     <div class="container">
         <h1>Edit <%= book.title %> Book</h1>
         <form method="POST" action="/edit/<%= book._id %>">
             <label for="title">Title:</label>
             <input type="text" id="title" name="title" value="<%= book.title %>" required>
             <label for="author">Author:</label>
             <input type="text" id="author" name="author" value="<%= book.author %>" required>
             <label for="price">Price:</label>
             <input type="number" id="price" name="price" value="<%= book.price %>" required>
             <label for="description">Description:</label>
             <input type="text" id="description" name="description" value="<%= book.description %>" required>
             <label for="edition">Edition:</label>
             <input type="text" id="edition" name="edition" value="<%= book.edition %>" required>
             <label for="aboutAuthor">About Author:</label>
             <input type="text" id="aboutAuthor" name="aboutAuthor" value="<%= book.aboutAuthor %>" required>
             <label for="publish">Publish:</label>
             <input type="text" id="publish" name="publish" value="<%= book.publish %>" required>
             <label for="status">Status:</label>
             <input type="text" id="status" name="status" value="<%= book.status %>" required>
             <div class="button-container">
                 <button class="button" type="submit">Save Changes</button>
                 <a class="button" href="/">Cancel</a>
             </div>
         </form>
     </div>
 </body>
 </html>
  -->


  <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"> <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
        }

        .container {
            max-width: 600px;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 50px auto;
        }

        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        form label, form input {
            display: block;
            width: 100%;
            margin-bottom: 10px;
            color: #333;
        }

        form input {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            font-size: 16px;
            color: #333;
            transition: border-color 0.3s;
        }

        form input:hover {
            border-color: #007bff;
        }

        .button-container {
            text-align: center;
            margin-top: 20px;
        }

        .button {
            display: inline-block;
            padding: 12px 24px;
            font-size: 16px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            outline: none;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s;
            margin-right: 10px;
        }

        .button:hover {
            background-color: #0056b3;
        }

        a.button {
            text-decoration: none;
            display: inline-block;
            padding: 12px 24px;
            font-size: 16px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            outline: none;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        a.button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Edit <%= book.title %> Book</h1>
        <form method="POST" action="/edit/<%= book._id %>">
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" value="<%= book.title %>" required class="form-control">
            </div>
            <div class="form-group">
                <label for="author">Author:</label>
                <input type="text" id="author" name="author" value="<%= book.author %>" required class="form-control">
            </div>
            <div class="form-group">
                <label for="price">Price:</label>
                <input type="number" id="price" name="price" value="<%= book.price %>" required class="form-control">
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <input type="text" id="description" name="description" value="<%= book.description %>" required class="form-control">
            </div>
            <div class="form-group">
                <label for="edition">Edition:</label>
                <input type="text" id="edition" name="edition" value="<%= book.edition %>" required class="form-control">
            </div>
            <div class="form-group">
                <label for="aboutAuthor">About Author:</label>
                <input type="text" id="aboutAuthor" name="aboutAuthor" value="<%= book.aboutAuthor %>" required class="form-control">
            </div>
            <!-- <div class="form-group">
                <label for="publish">Publish:</label>
                <input type="text" id="publish" name="publish" value="<%= book.publish %>" required class="form-control">
            </div> -->
            <div class="form-group">
                <label for="publish">Publish Year:</label>
                <input type="text" id="publish" name="publish" value="<%= book.publish %>" required class="form-control">
            </div>
            
            <div class="form-group">
                <label for="status">Status:</label>
                <input type="text" id="status" name="status" value="<%= book.status %>" required class="form-control">
            </div>
            <div class="button-container">
                <button class="button btn btn-primary" type="submit">Save Changes</button>
                <a class="button btn btn-secondary" href="/">Cancel</a>
            </div>
        </form>
    </div>
</body>
<script>
    $(function() {
        $("#publish").datepicker({
            dateFormat: "yy",
            yearRange: "1900:2099",
            changeYear: true,
            showButtonPanel: true,
        });
    });
</script>

</html>


  
